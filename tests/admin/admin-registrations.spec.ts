import { test } from "@playwright/test";

test.skip("Admin registrations section pending implementation", async () => {
  // TODO: Replace this with a real UI test once the registrations dashboard
  // is fully wired up and stable.
});
